// cpp program to perform division  for given two numbers.
#include <iostream>
using namespace std;
int main() {
    
    int a,b;
    float c;
    cin>>a>>b;
    c=a/b;
    cout<<"the division of two numbers = "<<c;
    return 0;
}

// cpp program to perform multiplication  for given two numbers.
#include <iostream>
using namespace std;
int main() {
    
    int a,b,c;
    cin>>a>>b;
    c=a*b;
    cout<<"the multiplication of two numbers = "<<c;
    return 0;
}

// cpp program to perform substraction  for given two numbers.
#include <iostream>
using namespace std;
int main() {
    
    int a,b,c;
    cin>>a>>b;
    c=a-b;
    cout<<"the substraction of two numbers = "<<c;
    return 0;
}

// cpp program to perform addition  for given two numbers.
#include <iostream>
using namespace std;
int main() {
    
    int a,b,c;
    cin>>a>>b;
    c=a+b;
    cout<<"the addition of two numbers = "<<c;
    return 0;
}